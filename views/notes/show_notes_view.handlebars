

    <div class="panel panel-default">
      <div class="panel-heading">
        <h3>Field Notes</h3>

      </div>
      <div class="panel-body">
        <table class="table table-striped table-hover ">
          <thead>
            <tr>
              <th>Contribtor</th>
              <th>Link</th>
              <th>Title</th>
              <th>Description</th>
              <th>Date</th>
               <th>Edit</th>
            </tr>
          </thead>
          <tbody>
          {{#each data}}
             

              <tr>
              <td>@ablue</td>
              <td><a href="#"><span class="glyphicon glyphicon-file"></span></a></td>
              <td> {{this.title}}</td>
              <td> {{this.description}}</td>
              <td> {{this.notedate}}</td>
               <td> 
               <!--form action="/update/{{this.id}}?_method=PUT" method="POST">
                     <input type="hidden" name="sleepy" value="true">
                     <button type="submit">Edit</button>
                   </form-->
                 <button ><a href="/notes/delete/{{this.id}}">delete</a></button>
                </td>
            </tr>
          {{/each}}

           <!--  <ul>
             {{#each cats}}
               {{#unless this.sleepy}}
                 <li>
                   {{this.name}}
           
                   <form action="/{{this.id}}?_method=PUT" method="POST">
                     <input type="hidden" name="sleepy" value="true">
                     <button type="submit">SLEEP TIME!</button>
                   </form>
                 </li>
               {{/unless}}
             {{/each}}
           </ul> -->
         
            
          </tbody>
        </table> 

      </div>
    </div>
 
    
  
  