<div class="jumbotron">
{{#each Project}}
  <h2> {{this.name}}</h2>
  <div class="container">
  <div class="row">
  <div class ="col-md-3"><img  src= "{{this.image_url}}"  height="150" width="150"> </div>
          <div class ="col-md-6">   
                  <div> Details: {{this.tagLine}} </div>
                  <div> Location: {{this.location}} </div>
                  <div> </div>
                  <a class="btn btn-success btn-lg">Create a Field Note</a></p>
            </div>      
  
     {{/each}}

 </div>
 </div>

  </div>


    <div class="panel panel-default">
      <div class="panel-heading">
        <h3>Field Notes</h3>
      </div>
      <div class="panel-body">
        <table class="table table-striped table-hover ">
          <thead>
            <tr>
              <th>Contribtor</th>
              <th>Link</th>
              <th>Title</th>
              <th>Description</th>
              <th>Date</th>
               <th>Edit</th>
            </tr>
          </thead>
          <tbody>
          {{#each data}}
             

              <tr>
              <td>@ablue</td>
              <td><a href="#"><span class="glyphicon glyphicon-file"></span></a></td>
              <td> {{this.title}}</td>
              <td> {{this.description}}</td>
              <td> {{this.notedate}}</td>
               <td> 
               <form action="/{{this.id}}?_method=PUT" method="POST">
                     <input type="hidden" name="sleepy" value="true">
                     <button type="submit">Edit</button>
                   </form>
                </td>
            </tr>
          {{/each}}

           <!--  <ul>
             {{#each cats}}
               {{#unless this.sleepy}}
                 <li>
                   {{this.name}}
           
                   <form action="/{{this.id}}?_method=PUT" method="POST">
                     <input type="hidden" name="sleepy" value="true">
                     <button type="submit">SLEEP TIME!</button>
                   </form>
                 </li>
               {{/unless}}
             {{/each}}
           </ul> -->
         
            
          </tbody>
        </table> 

      </div>
    </div>
 
    
  
  